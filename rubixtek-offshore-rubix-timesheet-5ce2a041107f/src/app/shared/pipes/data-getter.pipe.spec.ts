import { DataGetterPipe } from './data-getter.pipe';

describe('DataGetterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataGetterPipe();
    expect(pipe).toBeTruthy();
  });
});
